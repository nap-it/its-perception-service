version: '2.4'
services:
  camera:
    hostname: camera
    image: code.nap.av.it.pt:5050/mobility-networks/cps-v2/camera-adapter:dev
    cap_add:
      - "NET_ADMIN"
    volumes:
      - ./config.ini:/config.ini
    network_mode: host