version: '2.4'
services:
  radar:
    hostname: radar
    image: code.nap.av.it.pt:5050/mobility-networks/cps-v2/radar-adapter:dev
    # image: radar-adapter:test
    cap_add:
      - "NET_ADMIN"
    volumes:
      - ./config.ini:/config.ini
    network_mode: host
    ipc: host